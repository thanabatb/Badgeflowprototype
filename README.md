
  # badgeflow-prototype

  This is a code bundle for badgeflow-prototype. The original project is available at https://www.figma.com/design/abQl1gHjjNh8fTcK82syQe/badgeflow-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  